# 多图镜头 —— 前端交互设计说明

> 配套文档：《分镜设计系统-开发任务说明.md》《前端技术栈与工具链说明.md》
> 范围：仅宫格视图（H5）中「一个镜头包含多张图」的**交互与视觉层**设计。
> 数据层与 Blender 端的实现方案另行推演（当前版本已搁置，不在本文档范围内）。
> 本文档只定义前端需要什么数据形态作为输入（见第 6 节接口约定），不定义后端如何实现。

---

## 1. 需求背景

分镜工作中，一个镜头常有 2 张以上的图（多机位候选、起幅/落幅关键帧等）。
宫格视图必须满足：

1. 一眼看出「这几张图属于同一个镜头」
2. 不展开也能快速预览全部图
3. 需要时可以展开逐张查看/操作
4. 单图镜头与多图镜头在宫格中和谐共存，不破坏网格秩序

**上限设定：一个镜头最多 5 张图**（交互层硬上限，防止候选泛滥导致决策瘫痪）。

## 2. 卡片双态

多图镜头卡片有两个状态：**折叠态（默认）** 与 **展开态**。

### 2.1 折叠态 ——「一叠牌」

- 占宫格 **1 格**，与单图镜头卡片同尺寸，网格不被破坏
- 视觉为「一叠卡片」：主图（封面）在下，后续图依次向上/向右错位偏移少许（CSS transform 位移 + 阴影），露出窄边，张数越多露出的层边越多（最多露出 2~3 层即可，不用全露）
- **左上角小气泡角标**显示张数（如 `3`）
- **悬停横向预览**：鼠标悬停在卡片上，沿卡片横向移动时，根据鼠标 X 坐标在区间内实时切换显示的图（左端=第1张，右端=第N张），切换即时、无渐变延迟，手感类似电商商品图扫视
- 双击 → 进入展开态

### 2.2 展开态 ——「占 N 格」

- 该镜头的 N 张图**各自独占一个格位**，在宫格流式布局中连续排列（有几张占几格）
- **多图镜头的图比单图镜头小一圈**（格位相同，图面内缩，例如内缩 8~10%）
- **N 张图共同垫一个深色底衬**（一个跨越 N 格的深色圆角容器，图置于其上）：
  - 视觉上成组，归属关系不言自明
  - 统一实拍图与占位格的质感基底
- 展开状态下可逐张操作（设封面、重拍请求、删除——操作项以右键菜单承载）
- 双击组内任意图或点击收起钮 → 回到折叠态（折叠后以封面图为主图）

### 2.3 缺图占位 ——「红格子」

- 若某张图数据存在但图片文件缺失/加载失败（误删、同步中断等异常），该格显示**红色占位格**（深红/警示色块 + 帧号），与实图同尺寸、同位置参与布局
- 红格子可交互（触发该帧号的重拍请求）

## 3. 动效规格

沿用既定动效栈（transform-only 动画 + FLIP + 弹簧物理）：

| 交互 | 动效 |
|---|---|
| 折叠 → 展开 | 一叠牌展开成一排：错位层以弹簧动画各自滑入目标格位，其余宫格卡片 FLIP 让位 |
| 展开 → 折叠 | 逆向收拢，非封面图滑回主图之下 |
| 悬停横向预览 | 无过渡动画，即时切换（保证跟手）；可叠极短（<80ms）的交叉淡化减少闪跳感 |
| 红格子 | 常态静态；可配轻微呼吸脉冲提示「待拍」，hover 时停止脉冲 |

折叠/展开的动效是本功能的招牌时刻，优先打磨。

## 4. 单图 / 多图共存规则

- 宫格按 `seq` 顺序流式排列；展开的多图镜头将其后所有卡片自然后推，不预留空洞
- 同一时间**允许多个镜头处于展开态**（不做手风琴互斥，避免用户在两个镜头间比对时反复展开）
- 展开状态是**视图状态**，随会话存在，不写库（刷新页面后恢复全折叠；如后续有需求再考虑记忆）

## 5. 交互入口汇总

| 操作 | 入口 |
|---|---|
| 预览多图 | 悬停横向移动（折叠态） |
| 展开 | 双击折叠态卡片 / 右键菜单「展开」 |
| 收起 | 双击组内图 / 右键菜单「收起」 / 底衬上的收起钮 |
| 设封面 | 展开态右键某图「设为封面」 |
| 拍屏/重拍请求 | 展开态右键某图或红格子 → 发送对应槽位请求（后端行为另行定义） |
| 删除某张 | 展开态右键「删除此张」（需确认） |

## 6. 前端数据接口约定（前端视角的输入需求）

> 仅约束「前端需要拿到什么」，不约束后端存储形态。

每个镜头（shot）在前端数据层表现为：

```ts
interface Shot {
  id: string;
  seq: number;
  name: string;
  duration: number;       // 镜头时长（秒）。手填的创作决策字段，
                          // 前端禁止从帧号推导（见 6.2 权属说明）
  frames: Frame[];        // 单图镜头 = 长度 1 的数组；多图按 frame_no 升序排列
}

interface Frame {
  frame_no: number;       // 取景坐标：该图拍摄时所在的 Blender 帧号。
                          // 纯技术元数据，仅用于「跳回该构图」「重拍覆盖检测」
                          // 与多图的默认排列顺序；删除中间张不影响其他帧号
  imageUrl: string | null; // null 或加载失败 → 渲染红格子
  isCover: boolean;       // 是否封面（折叠态主图）
}
```

- 张数 = `frames.length`，前端不单独接收「张数」字段
- 缺图判断：优先以 `imageUrl` 为 null 判定；加载失败（onerror）兜底降级为红格子
- 所有图片 URL 带缓存版本参数（沿用既定方案：`?v=<updated_at>`）

### 6.2 帧号与时长的权属（重要约束）

| | `frame_no` | `duration` |
|---|---|---|
| 本质 | 图的取景坐标（技术元数据） | 镜头的节奏决策（创作行为） |
| 来源 | 拍屏时自动记录 | 人手填写 |
| 前端用途 | 多图默认排列顺序、双击跳回构图的请求参数 | 时间线条带宽度、时长显示 |
| 禁止事项 | 不承载叙事/节奏含义，**不得用于推导时长** | 不被任何自动逻辑覆盖 |

背景：分镜阶段 K 帧的帧号位置是构图探索的脚手架（随手摆放），不代表镜头节奏；
节奏必须由创作者手填。因此时间线视图及未来任何功能，一律以手填 `duration` 为准。

## 7. 待后端推演时回答的问题（占位清单）

以下为前端已暴露、需数据层/Blender 端方案落地时对齐的问题：

1. 缺图的权威状态来源（前端当前以 imageUrl 判定，后端如有状态字段可更准）
2. 拍屏请求端点的参数形态（shot_id + frame_no；同帧重拍时的覆盖确认由哪侧发起）
3. 封面标记的持久化位置
4. 双击跳回构图的请求端点（shot_id + frame_no → 切 Scene + 时间轴跳帧）
5. `duration` 的编辑入口与写回通道（手填字段，前后端均需编辑能力）
